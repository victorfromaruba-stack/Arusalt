# AruSalt website — project memory

AruSalt is a small, real hand-harvested sea salt brand from Aruba. This project is a single-file HTML + Tailwind CDN website (no build step, no framework, no custom backend). Two products exist today, sold in-store only: Pure Caribbean Sea Salt ($29) and Caribbean Bath Salt ($49). Online ordering is being added through Stripe Payment Links (not Shopify — that plan was replaced; AruSalt makes and ships the salt themselves, so a full e-commerce platform isn't needed, just checkout). See "Stripe checkout architecture" below.

## Image organization

All images live under `images/`, categorized by subfolder — `images/logo/` (wordmark, PWA icons, the cropped diamond mark used by the preloader), `images/hero/` (the full-bleed ocean coastline photo, reused smaller in the About Us section), `images/products/` (product jar photos and their decorative hover-swap overlays), `images/history/` (archival/history-section photography), `images/retailers/` (AI-generated decorative banner photos for the retailer cards — see "Retailer card banners" below). Responsive `srcset` variants (`{basename}-{width}w.{ext}`) live alongside their full-size original in the same category folder. When adding a new image, put it in the matching category folder (or create a new one if it doesn't fit an existing category) — don't drop it in the repo root.

## Content editing system (no-GitHub-access editors)

Some copy is editable without touching GitHub, via a small Decap CMS instance at `/admin`:
- `content/site.json` holds the editable fields (hero headline/subhead, announcement bar text, both products' description/price/net weight, the retailers list, newsletter blurb). Non-editable content stays hardcoded in `index.html` as before.
- A hydration script at the end of `index.html`'s `<script>` block fetches `content/site.json` on page load and patches it into elements tagged `id="cms-*"` (or `class="cms-marquee-line1/2"`), rebuilding `#cms-retailers-grid` entirely from the `retailers` array. If the fetch fails for any reason, the static HTML already in the page is the fallback — the page never breaks from this.
- `admin/config.yml` defines the Decap CMS field schema (must stay in sync with `content/site.json`'s shape, and with the `id`s in `index.html` that the hydration script reads).
- `admin/index.html` is the CMS login/editor page. It has its own separate CSP meta tag (more permissive than the main site's, since it needs to load the Decap CMS bundle from unpkg and Netlify Identity) and is excluded from search indexing (`robots.txt` + `noindex` meta).
- Backend is `git-gateway` + Netlify Identity — editors log in with an email/password Netlify Identity account (invited by the site owner), never a GitHub account. Netlify proxies the actual git commits using the owner's GitHub authorization, done once during setup.
- `publish_mode: editorial_workflow` — edits sit as drafts/in-review before anything merges to `main` and goes live, rather than publishing instantly. Deliberate given the owner's security priorities; can be simplified to `publish_mode: simple` later if the review step proves like unnecessary friction.
- When adding a new editable field: add it to `content/site.json`, add the matching field block to `admin/config.yml`, tag the target element in `index.html` with a `cms-*` id/class, and add the corresponding lookup in the hydration script.
- The hydration script also keeps the Product JSON-LD (`<script type="application/ld+json" id="product-schema">`) price fields in sync with `content/site.json`'s prices — parses the JSON, updates `offers.price` by matching product name against "Bath Salt", re-serializes. If a new CMS field should also be reflected in a JSON-LD block (schema.org data isn't otherwise touched by the hydration script), extend this same pattern rather than leaving structured data to silently drift from what an editor sets via `/admin`.

## Stripe checkout architecture

Online ordering is being wired up through **Stripe Payment Links**, not a custom checkout build and not Shopify (that plan was replaced — see priority #2). Decision record, since this matters if it's ever revisited: the site is static (GitHub Pages, no server), and a Stripe secret key can never live in client-side code, so any "real" custom checkout would need a backend to stand up and maintain. Payment Links need none — the owner creates/manages them directly in the Stripe Dashboard (shipping rates, international shipping, tax-inclusive pricing all configured there, not in this repo), the same self-serve spirit as the Decap CMS for copy.

- Each product in `content/site.json` (`products.seaSalt`, `products.bathSalt`) has a `buyLink` field (also in `admin/config.yml`), CMS-editable, empty string by default.
- The hydration script's `activateBuyLink()` function (search `index.html`) only acts if `buyLink` matches `/^https:\/\//i` — same validation convention already used for the retailers' `link` field. Empty/missing `buyLink` leaves the page exactly as it ships today (default is always safe).
- When a real link is set: the product's CTA (`id="cms-seasalt-cta"` / `id="cms-bathsalt-cta"`, currently "Find a Retailer" → `#retailers`) becomes a same-tab "Buy Now" link to Stripe's hosted checkout page (not `target="_blank"` — a purchase action should feel like part of the site's own flow, unlike the external informational links elsewhere on the page which do open in a new tab); the product card's "In Stores" badge (`id="cms-seasalt-badge-text"` / `id="cms-bathsalt-badge-text"`) becomes "In Stores & Online"; and the Product JSON-LD's `offers.availability` flips from `InStoreOnly` to `InStock` with `offers.url` set to the buy link, so Google's structured data stops claiming in-store-only once that's no longer true.
- The "AVAILABLE EXCLUSIVELY IN STORES" marquee line is deliberately *not* wired to auto-change when a buy link goes live — it's its own independent CMS field (`marquee.line2`), left to the owner to update on their own timing (e.g. to test a buy link before flipping the public messaging).
- `stripe-integrator` is the specialist subagent for this work (replaces the old `shopify-integrator`, deleted). Same non-fabrication rule as the old agent had for Shopify credentials: never invent a Stripe Payment Link URL, API key, or price ID — ask the owner for the real one.
- **Not yet done, needs the owner:** actually create the two Stripe Payment Links in their Stripe Dashboard (with international shipping rates and tax-inclusive pricing configured there) and paste the URLs into the CMS. Until that happens, both `buyLink` fields stay empty and the site behaves exactly as it does today.
- **Future products:** don't pre-build a generalized N-product system ahead of a real third product existing (see `stripe-integrator`'s own notes) — when one does, convert the Shop section to the same CMS-array-driven pattern already proven on Retailers (`content/site.json`'s `retailers` array + `#cms-retailers-grid`), rather than adding a fourth hand-tagged hardcoded card.

## The team

This project has specialist subagents defined in `.claude/agents/`. Delegate to them rather than doing everything in the main session:

- `stripe-integrator` — Stripe Payment Link wiring, product schema, checkout/e-commerce structure
- `copy-auditor` — matches and reviews on-page text against the real Shopify store
- `design-system-guardian` — read-only review of new UI against the design tokens below
- `accessibility-auditor` — read-only accessibility review
- `seo-performance-auditor` — meta tags, schema.org, canonical URL, performance review
- `qa-validator` — final tag-balance + leftover-placeholder check before calling anything done

Claude Code will auto-delegate to these based on their `description` fields, or you can invoke one explicitly, e.g. "Use the qa-validator subagent to check the last change."

Standard flow for any non-trivial change: make the edit → relevant specialist reviews it (design-system-guardian / accessibility-auditor / seo-performance-auditor as applicable) → qa-validator runs last, before telling me it's done.

## Design system — don't drift from this without flagging it to me

- **Fonts:** Space Grotesk for headings (h1–h4), Inter for body text
- **Colors:** ocean blue `#0ea5e9` (primary, used broadly), coral `#ef6a72` (secondary accent — used sparingly, not as a default)
- **Signature motif:** a small rotated-square "diamond" shape echoing the crystal-lattice logo mark, reused as section dividers. A second, intentional divider shape — a wave, echoing the logo's wave motif — is used specifically at two full-section seams (`.wave-divider`, before "Respect for land and sea" and before "History"); this coexists with the diamond by design (confirmed with the site owner), not an inconsistency to converge.
- **Icons:** hand-drawn inline SVG (no image files), thin-stroke (~1.3 stroke-width on a 24×24 viewBox), plain dark (`text-gray-900`) — matching the real Shopify store's icon style, not the site's blue/coral accents. Exception, confirmed with the site owner: functional/interactive UI icons (back-to-top arrow, FAQ accordion chevrons, sustainability checklist marks) use ocean-blue and a thicker 1.5–2px stroke instead — color signals interactivity/status for these specifically, distinct from purely illustrative icons.
- **Retailer card banners:** the retailer cards in "Find us locally in Aruba" use a full-width, full-bleed photographic banner (`images/retailers/`, 2.667:1 crop) instead of a line icon — each of the three cards has its own distinct, high-energy lifestyle photo (a palm tree framing an empty turquoise beach for T.H. Palm & Company, a family playing on the beach near a divi-divi tree for Trade Winds at Hilton Aruba, a woman in a flowing dress walking a dramatic rocky coastline for Trade Winds at Marriott Aruba). These are original AI-generated editorial photography, not real photos of the actual storefronts, not real customers, and not the retailers' own branding/logos — those still need the retailers' permission and aren't fabricated (see priority list below). `alt=""`/`aria-hidden="true"` since the banner is decorative atmosphere, not information the card's text doesn't already carry (the people depicted are generic/AI-generated, not real customers or endorsements). Uses the same rounded-3xl + overflow-hidden card pattern as elsewhere so the banner's square top corners clip to match the card shape — keep the `<img>` flush against the card's top/left/right edges (no wrapping padding), with a `srcset`/`sizes` responsive set (480w/800w/1400w base, `sizes="(min-width: 768px) 30vw, 100vw"`) if you add another one. The CMS-hydration template picks the right banner by matching "hilton"/"marriott" in the retailer name (falls back to the T.H. Palm image for anything else) — keep that in sync if a fourth distinct retailer image is ever added. AI-generated photos from this pipeline have repeatedly had a small white 4-pointed sparkle watermark baked into the same bottom-right corner position — on smooth backgrounds (sky, sand) a horizontal clone-patch from a nearby clean area works well; on busy/textured backgrounds (rocks, fabric folds) `cv2.inpaint` produces visible radial smearing artifacts and should be avoided in favor of a manually-placed feathered clone patch sampled from a same-material region of the same photo, verified by zooming into the corner before shipping.
- **Section backgrounds:** alternate white and a pale ocean-tinted blue (`#f4f9fc`) — never plain Tailwind gray. Non-`content-section` interstitial elements (e.g. the coral Gift Banner) are deliberately excluded from the alternation count; when inserting/removing a full section, recompute the white/pale sequence for everything downstream.
- **Preloader-to-hero handoff:** the preloader's pulsing crystal (`.loader-crystal`, wrapped in `.loader-crystal-wrap`) is `images/logo/arusalt-diamond-icon.png` — a pixel-perfect crop of the diamond mark taken directly from the real logo artwork (isolated via connected-component analysis so it excludes the nearby wave, not a hand-drawn SVG approximation of it). It's deliberately offset upward via `translateY` (two breakpoint values, matching the hero logo's own `w-[260px]`/`sm:w-[300px]` responsive sizing) so it lands exactly on top of the diamond mark within the hero logo — the logo is a diamond-over-wave-over-wordmark lockup, so the diamond's center isn't the logo image's own vertical center. Its own display width is also set per breakpoint (`121px`/`140px`) to match the diamond's actual proportional size within the hero logo at that width. When the preloader fades and the hero logo fades in, the crystal appears to stay in place while the wave and wordmark grow in around it, rather than jumping to a new position or size. If the logo artwork or its responsive sizing ever changes, regenerate `arusalt-diamond-icon.png` from the new artwork and recompute both the offset (crystal center should land at ~30.5% down from the top of the logo's own bounding box) and the width (diamond is ~46.7% of the logo's own rendered width) rather than leaving either stale.

## Ground rules

- Keep the single-file HTML + Tailwind-CDN approach unless I explicitly ask to change it.
- No new image files unless I ask for one.
- Never fabricate business details, prices, store credentials, or a canonical domain — ask me instead.
- Canonical domain is confirmed: **arusalt.com**. CNAME, canonical tag, og:url, and schema.org URLs are all aligned on it; robots.txt/sitemap.xml also point at it. Don't reintroduce arusaltaruba.com without asking first.
- Resolved: the live Shopify store (arusaltaruba.com) shows both products as "Sold out" — this is intentional, not a discrepancy to fix. Products are in-store-only for now; the Shopify listings are deliberately kept in place (sold out) so online ordering can be switched on later without rebuilding the store. Keep "AVAILABLE EXCLUSIVELY IN STORES" / "Find a Retailer" copy as-is until priority #2 below actually goes live.
- Run `qa-validator` after any HTML/CSS/JS change, before telling me something is done.
- `index.html` has a Content-Security-Policy meta tag. If you add a new external resource (script, stylesheet, font, fetch target), update the CSP to allow it explicitly — don't loosen it to a wildcard, and don't add `unsafe-eval` to the main site's CSP (only `admin/index.html`, a separate document, has that, because the Decap CMS bundle needs it).
- GitHub Pages can't serve custom HTTP headers, so `X-Frame-Options`, `X-Content-Type-Options`, and CSP's `frame-ancestors` directive can't be set from this repo at all — that's a platform limitation, not something to keep re-flagging as an open bug.
- **Tailwind JIT trap, easy to reintroduce:** any custom/brand color that isn't a real Tailwind config token (`theme-blue`, `coral`, `coral-pale`, etc.) silently gets zero generated CSS for variant classes built on it — `hover:`, `focus:`, `border-`, `shadow-{color}/{opacity}`, and similar — because Tailwind's CDN JIT only generates what it recognizes from its own palette. This has bitten `hover:text-theme-blue`, `border-theme-blue`/`focus:border-theme-blue`, and `shadow-theme-blue/20` so far (all now fixed with explicit CSS rules in the `<style>` block). When adding a new utility-lookalike class on a custom color, add the backing CSS rule in the same change and verify with `getComputedStyle()` in a real browser — don't trust that the class "looks like" a Tailwind utility. Note also that Tailwind's CDN-generated stylesheet is injected *after* this file's own `<style>` block, so for properties Tailwind sets via shared CSS custom properties (like `box-shadow`'s `--tw-shadow`/`--tw-shadow-colored` chain), a same-specificity override in `<style>` can silently lose the cascade — verify computed values, and reach for a direct property override (optionally scoped by id, with `!important` if needed) rather than trying to replicate Tailwind's internal custom-property mechanism.
- The legal policy modal locks the background with `inert` on a `backgroundRegions` array (nav wrapper, mobile menu, WhatsApp widget, back-to-top button, skip-link, `<main>`, `<footer>`) whenever the modal opens. Any new element added as a direct `<body>`-level sibling (not inside `<main>`/`<footer>`/nav/mobile-menu) needs to be added to this array too, or it stays focusable/interactive while the modal is open — this has been the single most common regression this project introduces when adding new floating/fixed UI. The mobile menu has its own equivalent (`menuBackgroundRegions`: `<main>`, `<footer>`, WhatsApp widget, back-to-top — deliberately *not* the nav wrapper, since the header stays visible above the menu and `menu-btn` itself must stay reachable to close it) — keep both lists in sync with whatever body-level siblings exist.
- Elements that are invisible-but-present (`opacity: 0`/`pointer-events: none`, not `display: none`) — `#back-to-top` near the top of the page, `#header-logo-link` before scrolling past the hero — must have their `tabIndex` toggled between `-1` and `0` alongside their visibility, or they stay keyboard-focusable while invisible with no visible focus indicator. Both now do this correctly; apply the same pattern to any new element using this show/hide technique.
- `#back-to-top` and `#whatsapp-widget` are both stacked on the **right** edge (`right-6`, back-to-top at `bottom-28` sitting above the WhatsApp button at `bottom-6`) — deliberately keeping the entire left edge free of fixed floating UI. This was a real bug once: back-to-top used to sit `bottom-6 left-6`, which visibly collided with the retailer cards' left-aligned "Visit site"/"Directions" links whenever a user scrolled (or anchor-linked) to a position where that row landed near the bottom of the viewport — an opaque circular button sitting on top of, and partially blocking taps on, real link text. If you ever add another fixed/floating bottom-corner element, keep it on the right stack (or verify it doesn't reintroduce a left-side collision) rather than defaulting to the opposite corner.
- Any JS that needs to `.focus()` an element immediately after a class change that flips it from `visibility: hidden` to `visible` (e.g. opening the mobile menu) needs to defer that focus call by **two** `requestAnimationFrame` calls, not zero and not one — verified empirically: a same-tick or single-rAF-deferred `.focus()` silently no-ops because the CSS-transition-driven visibility change hasn't committed yet. The mobile menu's open handler does this; reuse the pattern rather than rediscovering it.
- `focus:outline-none` on a Tailwind utility class beats the site's global `a:focus-visible, button:focus-visible, input:focus-visible` outline rule by CSS specificity (a class+pseudo-class selector outranks an element+pseudo-class one), regardless of source order. Don't add `focus:outline-none` to a focusable element unless it also supplies its own equally-visible focus style — verify with `getComputedStyle(el).outlineColor` after a real focus, not just visually.
- **No em dashes (—) in visitor-facing copy** — they read as an AI-writing tell. Fine in code/HTML comments (developer-only), never in text a visitor reads (headings, paragraphs, button/link text, aria-labels, alt text, JSON-LD `text`/`name` fields, `content/site.json`). Rewrite the sentence instead (a period, comma, colon, or "and"/"but" usually works better anyway). A full sweep was done once already; don't reintroduce the pattern in new copy.
- **Responsive images:** most photographic `<img>` tags carry `srcset`/`sizes` (480w/800w card-size variants, plus 768w/1280w for the full-bleed hero background) generated as `{basename}-{width}w.{ext}` alongside the original file, which stays as the largest/fallback source. When adding a new photo used at a "card" size (roughly half of a `max-w-5xl`/`max-w-6xl` 2-column grid), follow the same pattern: generate 480w and 800w variants, add `srcset` + `sizes="(min-width: 768px) 50vw, 100vw"` (adjust the `sizes` value if the layout differs). The two logo `<img>` tags are the deliberate exception — already sized correctly for their one use case, no variants needed.

## Current priority order

1. Match copy site-wide to the real Shopify store — `copy-auditor` (I'll provide screenshots or the live URL)
2. In progress: wire up Stripe Payment Links for both products — `stripe-integrator`. Code-side plumbing is done (see "Stripe checkout architecture" below); waiting on the owner to actually create the two Payment Links in their Stripe Dashboard and paste the URLs into `content/site.json`'s `products.seaSalt.buyLink` / `products.bathSalt.buyLink` via the CMS.
3. Once a buy link is live: the CTA/badge/JSON-LD update automatically (see below), but the "AVAILABLE EXCLUSIVELY IN STORES" marquee line is a separate, deliberately-not-automated CMS field (`content/site.json`'s `marquee.line2`) — update it yourself once you're ready, since you may want to test the buy link before changing that messaging. Once real online ordering is live for good, also revisit the "once online ordering is available" hedges throughout the legal modal (Terms §§2-5/9, Privacy, Refund — see item 14 below) and drop the qualifiers.
4. Smaller open items: newsletter form still has a placeholder `YOUR_FORM_ID` in its Formspree action attribute (needs the real form ID), parallax background not yet tested on a real older Android/iOS device. Deferred, needs real material from the owner before it can be built: founder/family photos, behind-the-scenes video, retailer storefront photos, real customer testimonials, press mentions — never fabricate these. Also deferred: a real language toggle (infrastructure would need to be built, but machine-translated copy shouldn't go live on a real business site without native-speaker review). (Done: dead footer "Search" link removed, `sitemap.xml`/`robots.txt` added, History-section images fixed after several were found to be mismatched product photos instead of the real archival photography, product cards enriched with real copy from the live Shopify store, all generic `IMG_NNNN` filenames still in active use renamed to descriptive names, Plausible analytics wired in — inert until an account exists, service worker added at `/sw.js` for offline/PWA support — deliberately excludes `/content/` and `/admin/` from caching so CMS edits always stay fresh, high-contrast toggle added and persisted via localStorage, back-to-top button, FAQ section with matching schema.org FAQPage markup, Salt Comparison table, Sustainability section, "From Ocean to Jar" process timeline, retailer cards now include a "Directions" link to Google Maps alongside the existing site/social link.)
5. Resolved: the ~28 unused leftover image files that used to sit in the repo root (old `ChatGPTImage...`/`IMG_21xx`/`IMG_22xx`/`IMG_23xx` files, stock photos, the superseded `IMG_2167.webp`/`IMG_2168.webp`/`IMG_2242.jpeg`) plus a stray `claude-team.zip` have been deleted, with the owner's explicit go-ahead — confirmed via grep first that none were referenced anywhere in `index.html`, `content/site.json`, `admin/`, `site.webmanifest`, `sw.js`, `robots.txt`, or `sitemap.xml`. The repo root now only contains images actually in use.
6. Flagged, not changed: the ocean blue `#0ea5e9` used for link/CTA text on white (`.text-theme-blue`, `.nav-link.active`) measures ~2.8:1 contrast, below WCAG AA's 4.5:1 for normal text. This is the core brand color used broadly per the design system above, so I didn't unilaterally darken it — that's a brand call, not a bug-fix call. If you want it addressed, options are a slightly darker blue for small text specifically (keeping `#0ea5e9` for large elements/backgrounds, which only need 3:1) or accepting it as a deliberate brand choice. The same ~2.8:1 ratio also recurs as white text on the ocean-blue announcement marquee/newsletter "Sign up" button (inverse pairing, same root cause) — bundled with this same open decision, not a separate issue.
7. From a full very-deep code review pass (everything below is now fixed unless marked otherwise): mobile menu gained `aria-expanded`/`aria-controls`, a dialog role, an `inert`-based background lock matching the legal modal's pattern, an Escape handler, and focus movement in/out (see Ground rules for the double-rAF timing note this required); `#header-logo-link` and `#back-to-top` now correctly toggle out of the tab order while invisible; removed two `focus:outline-none` instances that were silently killing the global focus ring; footer policy triggers changed from non-navigating `<a href="#">` to real `<button>`s; Salt Comparison table got a proper accessible name; two purely-decorative hover-swap product images got `alt=""`; legal modal's panel headings demoted from h2 to h3 (redundant with the modal's own h2 title); fixed a grammar error in the History section; Product JSON-LD price now syncs with CMS edits (see Content editing system above); LocalBusiness JSON-LD gained `url`/`priceRange`; `og:image`/`twitter:image` swapped from a portrait product photo to the landscape coastline photo (correct aspect ratio for social cards); removed a redundant font preload, added a `cdn.tailwindcss.com` preconnect (and allowed it in the CSP's `connect-src`, since preconnect hints are subject to that directive too); particle canvas now stops if `prefers-reduced-motion` is toggled mid-session (was previously only checked once at load); two footer text elements bumped from gray-500/600 to gray-400 for AA contrast against the dark background; the "In Stores" product-badge text got its own higher-contrast coral shade (`.text-coral-contrast`, `#c73e46`) instead of the base brand coral, which only hit ~3:1; removed a misleading `cursor-pointer` from product cards (only the CTA link inside is actually clickable, not the whole card); "THE SALT" heading changed to title case to match its siblings.
8. Resolved via owner confirmation (were flagged in item 7 above, now settled): contact email corrected from `support@psaruba.com` to the real `info@arusalt.com` everywhere (JSON-LD, legal modal's Contact and Refund panels); the shared Instagram handle across both hotel retailer listings is confirmed correct, not a copy-paste slip, no change needed; the LocalBusiness/legal-modal `postalCode` (`"00000"`) is removed entirely rather than kept as a placeholder, since Aruba has no postal code system and it's not a required schema.org field. The manifest icon problem is fixed too: `arusalt-logo-192.png` and `arusalt-logo-512.png` (new files, padded-to-square from the original logo, transparent background) now back `site.webmanifest`'s icons array and the `apple-touch-icon` link — the first exception to "no new image files" so far, done with explicit go-ahead. `sw.js`'s `CACHE_NAME` was bumped to `arusalt-v2` alongside this since `site.webmanifest`'s content changed and is cached cache-first — bump `CACHE_NAME` again on any future deploy that changes a same-named cached asset in place, or returning visitors keep the stale cached version indefinitely.
9. Image compression pass (with explicit go-ahead, same visual content, no new files except the two manifest icons above): `arusalt-logo.png` resized from 1207×988 to 600×491 (its largest real on-page use is ~300px wide) — 317KB → 117KB. `arusalt-pure-sea-salt-jar.webp`, `arusalt-caribbean-bath-salt-jar.webp`, and `aruba-coastline-aerial.webp` re-encoded at a leaner WebP quality with unchanged dimensions (the coastline photo's dimensions were deliberately preserved since it's also used full-bleed as the fixed hero background). `salt-crystals-macro.jpeg` and `hand-holding-sea-salt-ocean.jpeg` re-encoded at a leaner JPEG quality, also dimensions unchanged. All six were visually spot-checked after compression for artifacts before committing. The other in-page photos were already reasonably sized/compressed and left untouched.
10. Resolved, with explicit go-ahead: full `srcset`/`sizes` responsive images across all 13 photographic `<img>` tags (see Ground rules above for the pattern), adding 18 new resized variant files — verified in a real browser that mobile viewports correctly pull the smaller variants and desktop pulls the larger ones, with zero broken images. Also resolved: a full copy sweep removed every em dash from visitor-facing text (FAQ answers and their JSON-LD twins, product descriptions, retailer names/descriptions, timeline captions, comparison-table cells, sustainability copy, the newsletter success message) — rewritten to read naturally instead (see the em-dash ground rule above). Also resolved, with explicit go-ahead: the ~28 unused leftover image files and `claude-team.zip` noted in item 5 are now actually deleted (confirmed unreferenced first), not just flagged.
11. Resolved: the site had two consecutive sections — "Harvested with respect for land and sea" (a 3-icon-badge layout: Hand-harvested/Sun-dried/Pure and Natural) and "A lighter footprint, by design" (Sustainability: 2-column text + checklist) — making the same three claims back-to-back, reworded. Merged into one section (kept the Sustainability layout, which has more depth — a Netherlands-lab-testing trust signal the other section lacked — plus its wave-divider seam treatment and eyebrow text changed to "Respect for land and sea" to carry over that concept). Background alternation for every section downstream (retailers, history, faq) was recomputed and flipped accordingly; history's wave-divider fill color updated to match its new background. Also fixed as part of this: the checklist card's background was `bg-[#f4f9fc]` sitting inside a section whose own background was also `#f4f9fc` post-merge (the merged section took over the pale slot) — changed to `bg-white` so the card still visually contrasts against its section instead of blending in. If you ever revisit this section, note the left-column narrative and the right-column checklist still restate the same three points in different words (paragraph vs. scannable bullet) — a common, likely-fine narrative+recap pattern, but flagged in case you want to differentiate them further.
12. Resolved: two archival History-section photos (now named `salina-ranchoe-salt-flats-1924.webp`, `aruba-salt-workers-pouring-mound-1923.jpg` — see item 14 for the rename) had a white/cream border baked into the source photo itself (a scan of the physical photo with its own paper margin, one also had a "1923" caption and decorative circle in that margin), visible as a white gap around the photo inside its rounded card despite `object-cover`. Cropped both down to just the photo content in place (detected the border via a brightness/texture-variance threshold, not a fixed pixel guess), regenerated their `srcset` variants at the new dimensions, and updated `index.html`'s width descriptors to match. If a future archival photo has the same "scanned photo with paper border" issue, use this same crop-out-the-border approach rather than trying to fix it with CSS (the border is pixel content, not a CSS border/padding).
13. Resolved through five passes: retailer cards (T.H. Palm & Company, Trade Winds at Hilton Aruba, Trade Winds at Marriott Aruba) looked empty with just a small line icon. First pass wrapped the icon in `.feature-icon-badge` and gave the two Trade Winds cards a palm-tree icon instead of a generic building icon — still felt thin. Second pass replaced the icon with a full-width hand-drawn SVG scene illustration per card (brand-palette-only) — still read as decorative filler rather than "alive." Third pass replaced the SVG illustrations with full-width AI-generated photorealistic "busy scene" banners (boutique storefront shot through palm fronds, full beach club with umbrellas) shared identically between both Trade Winds cards. Fourth pass replaced those busy scenes with minimalist single-subject photography (one rattan chair, one straw umbrella, one lounge chair) per the site owner's "less stuff, Apple-like" direction — the owner liked the restraint but found the result not eye-catching enough. Fifth pass (what's live now, see the "Retailer card banners" design-system entry above): replaced the minimalist shots with higher-energy editorial lifestyle photography the owner sourced directly (a palm tree framing an empty beach, a family playing near a divi-divi tree, a woman in a flowing dress on a rocky coastline) — the owner explicitly specified the palm-tree beach photo go to T.H. Palm & Company; the assignment of the other two to Hilton vs. Marriott was my own judgment call (family/resort feel → Hilton, more upscale/dramatic coastline → Marriott), not owner-specified, so revisit if they want it flipped. This is deliberately *not* an attempt at the real retailers' actual logos/photos, and the people shown are AI-generated, not real customers or endorsements — those belong to real businesses and still need their permission, which is a separate, still-open item (get the real files from each retailer, or take/license storefront photos yourself, and they drop into these same card slots — same "needs real material from the owner" category as founder photos/testimonials in item 4 above).
14. Resolved: four image filenames that were vague or easy to confuse with a similarly-named sibling got renamed for clarity (all references in `index.html` updated to match, confirmed nothing else in the repo pointed at the old names): `salt-varieties-line.webp` → `bath-salt-blend-varieties-flatlay.webp` (it's a flat-lay of five salt/spice blend piles, used as the Bath Salt jar's hover-swap image); `aruba-salt-mound-1923.jpg` → `aruba-salt-workers-pouring-mound-1923.jpg` and `aruba-salt-harvesters-1923.jpg` → `aruba-salt-harvesters-group-portrait-1923.jpg` (both previously shared the generic `aruba-salt-*-1923` pattern, easy to mix up when skimming the folder — one shows workers actively pouring salt onto a mound, the other a posed group portrait on top of a salt pile); `salina-ranchoe-1924-archival.webp` → `salina-ranchoe-salt-flats-1924.webp` (the word "salt" wasn't in the old name at all). Also resolved: the legal modal's Refund Policy, Privacy Policy and Terms of Service were single-paragraph stubs, noticeably thinner than the real depth of arusaltaruba.com's live policy pages. Rewrote all three to full multi-section text at the same depth (Terms now has all 24 sections the live site's does), fetched directly from the live store for structure/content, but adapted in two ways rather than pasted verbatim: contact facts use this site's already-corrected values (`info@arusalt.com`, "L.G. Smith Boulevard 95, Noord, Aruba" — re-confirmed with the owner as correct, not the live store's `support@psaruba.com`/"Alto Vista 53, 00000 Noord, Aruba"), and language about orders/shipping/checkout is phrased as forward-looking ("once online ordering is available") rather than claiming this domain already processes payments, since AruSalt is still in-store-only here (see item 2 above) — avoids the policies quietly overclaiming functionality the site doesn't have yet. Once Stripe checkout actually goes live (priority #2), revisit those forward-looking phrases and drop the "once available" qualifiers. (Terms §9 "Relationship with Shopify" was later updated to "Relationship with Stripe" when the checkout plan changed — see "Stripe checkout architecture" below.) A `copy-auditor` pass on the first draft caught and fixed three factual-overreach issues before this shipped: Terms §1/§4 referenced a customer account system that doesn't exist anywhere on the site (no login, no account area) and, unlike every other checkout-related sentence in the same document, weren't hedged with "once available" — trimmed to remove the account references entirely rather than add a hedge, since accounts aren't an announced future feature (only Shopify checkout is); Privacy's info-we-collect list mentioned "our contact form," which doesn't exist (only the newsletter form and WhatsApp do); and Privacy's opt-out section referenced "the unsubscribe link in our newsletter emails," which overstates things since no outbound newsletter emails are currently sent (the newsletter form's Formspree endpoint is still the `YOUR_FORM_ID` placeholder from item 4). Also normalized Terms of Service's headings from generic Shopify-template ALL-CAPS "SECTION N - X" styling to the same warm sentence-case numbered style as Privacy/Refund's headings ("1. Access", "9. Relationship with Shopify"), since the template casing read as unedited boilerplate next to the other two tabs' voice.
15. Resolved from a full site-wide audit (5 parallel specialist passes: accessibility, SEO/performance, design-system, copy, technical/security — one flagged concern from two of the five turned out to be a false positive, misreading the "fourth pass → fifth pass" retailer-image history in item 13 as current guidance contradicting live state, when the fifth pass description was already correct and current): Refund Policy's first six paragraphs were unhedged present-tense ("you have 30 days after receiving your item") contradicting the FAQ's "not yet" on online ordering and the hedging pattern already applied to Terms/Privacy — restructured to lead with the actual current state (in-store purchases only) and hedge the rest as "once online ordering is available," matching the other two tabs; added a "Last updated" date to Terms and Refund (only Privacy had one, despite Terms §23 telling visitors to check the page periodically with no date to check against); "Salina Rancho" typo (missing the trailing "e", inconsistent with "Salina Ranchoe" one sentence later and the image filename); "flavour" → "flavor" (`content/site.json` and its rendered instance, the sole British-spelling outlier against 7 other "flavor" instances); lowercase "caribbean" → "Caribbean"; two missing commas; meta description's "finishing salts" (plural) narrowed to "sea salt" since only the Pure Caribbean Sea Salt is actually a finishing salt, not the Bath Salt; added a `404.html` (GitHub Pages was falling through to its generic branded 404, no way back to the real site); favicon `<link>` now points at the existing square `arusalt-logo-192.png`/`-512.png` icons instead of the non-square wordmark; removed the unused `data:` token from CSP's `img-src` (verified no `data:` URIs exist anywhere) — note the `connect-src https://cdn.tailwindcss.com` entry looked dead to two auditors but is NOT: it's required for the `<link rel="preconnect">` hint to that domain to work, per item 7's own documented reasoning, so it stayed; added `og:site_name`/`og:locale`; escaped `&` → `&amp;` in the three static Google Maps href attributes (HTML-validation nit, browsers tolerated it but it wasn't spec-clean); retailer card banner images gained an `aspect-[400/150]` wrapper div (matching the aspect-ratio-wrapper pattern used by every other photo on the site) since they were the only images on the page with no reserved space, risking layout shift as each lazy-loads into view; extended the CMS-hydration script to also recompute `LocalBusiness` JSON-LD's `priceRange` from both current product prices (was hardcoded `"$29-$49"` and silently drifting since only the `Product` schema's price was covered by the existing sync logic — gave the `LocalBusiness` block an `id="localbusiness-schema"` to hook into); recompressed the three retailer photos (quality 82 → 72, ~25-30% smaller, spot-checked for artifacts) since they'd been missed by the earlier image-compression pass and were notably heavier per-pixel than the rest of the site's photos; gated `.hover-lift` and all `group-hover:scale-*`/`hover:scale-*` transforms behind `prefers-reduced-motion` (previously only `animate-on-scroll`/`lazy-blur`/marquee/particle-canvas/ken-burns/loader were covered); gave the retailer card action links and the footer high-contrast toggle `py-2 -my-2` (invisible padding that doesn't shift layout) since their tap targets were ~14-18px, under WCAG's 24px minimum; promoted the Gift Banner's orphaned `<h3>` (governed by no `<h2>`, sitting between two sections as a bare `<div>`) to `<h2>`; bumped skip-link's `z-index` from 200 to 10000 so it's visible above the preloader's `z-index: 9999` if a keyboard user tabs before the preload finishes; changed the closing CTA section's `h-[50vh]` to `min-h-[50vh] py-16` so its text can't overflow/overlap the footer at high browser zoom or large text settings; added a visually-hidden `<h2 class="sr-only">Footer</h2>` landmark heading and promoted the footer's "Customer Help"/"Store Policies" `<h4>`s to `<h3>` to match their sibling "Join AruSalt" column (three parallel columns now share one heading level); added `aria-current="true"` alongside the existing color-only `.nav-link.active` scroll-spy state; removed leftover `delay-75`/`delay-150` classes from two of the three retailer cards (a holdover from an earlier entrance-animation pass that no longer applies since the cards don't use `animate-on-scroll` — was making their hover-shadow lag behind the third card for no reason); gave each retailer card's text block `flex flex-col flex-1` and its link row `mt-auto` so the "Visit site/Directions" links align to the same vertical position across all three cards regardless of description length; normalized the retailer links' arrow icon from `stroke-width="2"` to the site's standard `1.3` (kept the existing blue-vs-gray color distinction between the primary and "Directions" links, since that hierarchy looks deliberate, not accidental); changed the Gift Banner's icon from coral to `text-gray-900` (it was the only decorative icon anywhere on the page not using the plain-dark convention, sitting in a banner that already uses coral twice); normalized the diamond-divider spacing recipe (three different implementations converged on the canonical "unmargined h2 + `diamond-divider mt-6`" pattern); normalized the two-column narrative sections' grid gap (`gap-20` on About/The Salt was inconsistent with `gap-16` used by the other five sections using the same layout pattern — changed to match the majority). Flagged but deliberately not auto-fixed, since these are judgment calls for the owner: the Gift Banner's coral CTA button also measures ~3:1 contrast, same root cause as the already-open blue-contrast decision in item 6 — folded into that same open item rather than treated as a new one-off; three different visual treatments exist for the same repeated "Find a Retailer"/CTA button pattern across the page (`rounded-xl` outlined blue vs `rounded-full` solid coral vs `rounded-full` solid white) — a real inconsistency, but converging them is a bigger design decision than an audit-driven fix should make unilaterally; the 24-section Terms of Service reads as a visually undifferentiated wall of text (no dividers between numbered sections) — accessibility-wise this is fine (screen readers navigate via heading rotor same as any long document), so this is a polish opportunity, not a fix; a handful of interactive icons (legal modal's Back chevron, footer contrast toggle) partially-but-not-fully follow the "functional icons are ocean-blue + thick stroke" exception list — worth a decision on whether to formally add them to that list or bring them back to plain chrome styling; H2 sizes split into two scales (`text-4xl md:text-5xl` vs `text-3xl md:text-4xl`) that roughly tracks "core narrative" vs "utility/reference" sections but isn't documented anywhere as intentional; `LocalBusiness` may be a slightly better-fit schema.org type than `Organization` given AruSalt sells only through third-party retailers and never describes its own address as a place customers visit — a schema-type question, not a bug.
16. Resolved: a keyword-targeted SEO pass after the owner asked to rank for "sea salt Aruba" and tourist-intent searches (a 41-phrase keyword list across branded/product/gift-souvenir/location/heritage/sustainability clusters was drafted first to guide the changes). Title tag tightened to `AruSalt - Hand-Harvested Sea Salt from Aruba` (first landed on adding "& Gifts" to the title for gift-intent coverage, but a `copy-auditor` pass caught that "Sea Salt & Gifts" reads as a second product category AruSalt doesn't have, so it was reverted); meta description rewritten to cover the "sold at retail partners near Palm Beach... gift or souvenir" angle without overclaiming online availability; `og:description`/`twitter:description` were initially left on stale pre-SEO-pass copy and had to be brought in line with the new meta description in a follow-up fix (also fixed a leftover "finishing salt" (singular product) overclaim in those two tags matching the same fix already applied to the meta description). Added a 7th FAQ entry, "Does AruSalt make a good souvenir or gift from Aruba?", synced verbatim between the visible accordion and the FAQPage JSON-LD (matching the existing pattern of an in-HTML link to `#retailers` that the JSON-LD twin renders as plain text) — this directly targets the gift/souvenir search cluster in a form Google can surface as a rich FAQ result, rather than trying to cram every keyword into the title tag. Flagged to the owner as the highest-leverage next step, since it's outside what editing this repo can achieve: a Google Business Profile listing (local-intent searches like "sea salt Aruba" often surface the Maps/Local Pack ahead of organic results, and this site has no listing) and backlinks from the three retail partners' own websites (none currently appear to link back to arusalt.com) — both real off-page factors, not something an on-page SEO pass can fix.
